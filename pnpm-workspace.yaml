packages:
  - "apps/*"
  - "packages/*"
# Post-install build scripts allowed to run (pnpm blocks all others by default).
# Prisma generates its client; esbuild compiles its native binary for Vite/Vitest/tsx.
onlyBuiltDependencies:
  - "@prisma/client"
  - "@prisma/engines"
  - esbuild
  - prisma
