/**
 * Database seed entrypoint.
 *
 * No domain models exist yet (init-monorepo is scaffold only). The first
 * capability migration will add seeding here.
 */
async function main(): Promise<void> {
  console.warn("[db:seed] No models to seed yet — scaffold only.");
}

main();
