# Repositories

Data-access layer: the only place that talks to Prisma (`@nomadhome/db`). Exposes
intention-revealing methods to services and hides query details. See
openspec/project.md §5.

Empty until the first capability ticket lands.
