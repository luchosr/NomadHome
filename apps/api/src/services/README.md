# Services

Application/business-logic layer: orchestrate use cases, enforce invariants, and
call repositories. Services are framework-agnostic (no Express types) so they can
be unit-tested in isolation. See openspec/project.md §5.

Empty until the first capability ticket lands.
